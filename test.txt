num;
num = 4;
num = 4.f;
num = 4.0;

num = int;
num = float;
num = double;

num = int(7);
num = float(7);
num = double(7);

num : 4;
num : 4.f;
num : 4.0;

num : int;
num : float;
num : double;

num = "";
num = "Hello World";
num = "the message say \"hellow world\" here i am.";
num = "HELLOW" + " " + "WORLD";

str : "";
str : "Hello World";
str : "the message say \"hellow world\" here i am.";
str : "HELLOW" + " " + "WORLD";

var = 1.0 / (1.0 + x * c + y * b * b );
var = k += 5 / (1.0 + x * c + y * b * b );


 // static function declaration, that takes dynamic parameters
add : function () 
{
}

const add : function () 
{
}

// static function declaration, that takes static and dynamic parameters
const add : function ( x, y:float, const x, const y:float, a:[], const a:[] ) 
{

}

const main : function()
{
    file = open( "somefile/somewhere/image.jpu" );
    size = sizeof( file );
    data = []( size );
    fread( data, size, file );
    close( file );

    print( data );
    token: "";

    if( n == p )
    {
        n = k;
    }
    else if( n == k )
    {
        n = p;
    }
    else
    { 
        n = u;
    }

    for( i=0; i<size; i++)
    {
        k = data[i];
        if( k == " " )
        {
            break;
        }
        token += k;
    }

    while( p < 10 && k > p )
    {
        d = distance( a, b );
    }

    print( token );
}
